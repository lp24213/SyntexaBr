from typing import Any


class VideoAnalysis:
    """
    Stub para análise de vídeo (monitoramento, segurança, etc.).
    """

    def analyze(self, video_bytes: bytes) -> dict[str, Any]:
        return {
            "ok": False,
            "note": "VideoAnalysis ainda não está integrado a um modelo real.",
        }

