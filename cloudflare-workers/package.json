{
  "name": "syntexa-gateway",
  "version": "1.0.0",
  "description": "Cloudflare Workers gateway for Syntexa",
  "main": "src/index.js",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
